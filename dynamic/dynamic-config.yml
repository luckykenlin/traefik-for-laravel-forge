http:
  #region routers
  routers:
    easywms:
      entryPoints:
        - "https"
      rule: "Host(`abc.lkkus.app`)"
      middlewares:
        - testHeader
        - https-redirectscheme
      tls: {}
      service: easywms

  #endregion
  #region services
  services:
    easywms:
      loadBalancer:
        servers:
          - url: "https://httpbin.org"
        passHostHeader: true
  #endregion
  middlewares:
    testHeader:
      headers:
        customRequestHeaders:
          X-Forwarded-Host: "httpbin.org" # Adds
        customResponseHeaders:
          X-Custom-Response-Header: "value" # Remove
    https-redirectscheme:
      redirectScheme:
        scheme: https
        permanent: true