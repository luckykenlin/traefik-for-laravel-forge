http:
  #region routers
  routers:
    easywms:
      entryPoints:
        - "https"
      rule: "Host(`abc.lkkus.app`)"
      middlewares:
        - testHeader
        - https-redirectscheme
      tls: {}
      service: easywms

  #endregion
  #region services
  services:
    easywms:
      loadBalancer:
        servers:
          - url: "https://easywms.app"
        passHostHeader: true
  #endregion
  middlewares:
    testHeader:
      headers:
        customRequestHeaders:
          X-Forwarded-Host: "easywms.app" # Adds
        customResponseHeaders:
          X-Custom-Response-Header: "value" # Remove
    https-redirectscheme:
      redirectScheme:
        scheme: https
        permanent: true