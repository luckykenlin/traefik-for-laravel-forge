http:
  routers:
    netlify:
      entryPoints:
        - "http"
        - "https"
      rule: "Host(`lkkus.app`)"
      tls: {}
      service: netlify

  services:
    netlify:
      loadBalancer:
        servers:
          - url: https://www.0degreect.com #SSG website hosting via netlify
        passHostHeader: false